// import routesConfig from 'app/fuse-configs/routesConfig';

const initialState = {};

export const routes = function (state = initialState, action) {
    switch ( action.type )
    {
        default:
        {
            return {...state};
        }
    }
};